{
  "name": "fastify-plugins-test",
  "version": "1.0.0",
  "description": "Test fastify-plugins",
  "main": "server.ts",
  "types": "index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "rm -rf .build && tsc-watch --onSuccess \"node .build/server.js\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "fastify": "^4.0.0",
    "fastify-plugin": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.41",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.7.3"
  }
}
